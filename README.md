# ipl-player-availability
An event-driven player availability pipeline for IPL analytics that ingests official cricket news, extracts structured availability events using deterministic parsing, and builds a reliable historical dataset for machine learning and match prediction.
